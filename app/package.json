{
  "name": "docdoc",
  "version": "0.1.0",
  "description": "AI-enabled document scanning and management platform",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "for t in qrbill invoices blank pipeline; do ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron ../tests/test_$t.js || exit 1; done",
    "postinstall": "electron-rebuild -f -w better-sqlite3"
  },
  "license": "MIT",
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "electron": "^37.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "jpeg-js": "^0.4.4",
    "pdfjs-dist": "^4.10.38",
    "pngjs": "^7.0.0"
  }
}
